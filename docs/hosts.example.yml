all:
  hosts:
    example.ru:

  vars:
    ansible_user: root
    ansible_python_interpreter: /usr/libexec/platform-python

  children:
    nextcloud:
      hosts:
        example.ru:

    docker:
      hosts:
        example.ru:
